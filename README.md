# UrlShortner
A scalable URL Shortener application built using Spring Boot that converts long URLs into short, shareable links. It supports redirection, click tracking, and efficient storage using MySQL. Designed with RESTful APIs, it ensures fast performance and can be extended with authentication and analytics features.
